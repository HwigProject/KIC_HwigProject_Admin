<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hwig.admin.mappers.productMapper">
	<!-- 상품 등록 -->
	<insert id="add">
		insert into tbl_prd_b
		( prd_id, category_id, sel_id, prd_ea, prd_name, prd_thumb, prd_comment, prd_img,
			prd_stock, prd_price, prd_kg, prd_from, prd_wrap, prd_info)
		values((select nvl(max(prd_id)+1, 1) from tbl_prd_b), #{category_id}, #{sel_id}, #{prd_ea}, #{prd_name}, #{prd_thumb}, #{prd_comment}, #{prd_img},
			#{prd_stock}, #{prd_price}, #{prd_kg}, #{prd_from}, #{prd_wrap}, #{prd_info})
	</insert>
	
	<!-- 상품 등록 예정 리스트 조회 -->
	<select id="waitList" resultType="com.hwig.admin.product.ProductVO">
		select prd_id, category_id, sel_id, prd_name, prd_price, prd_comment from tbl_prd_b tpb order by tpb.prd_id desc
	</select>
	
	<!-- 등록된 상품 목록 조회 -->
	<select id="list" resultType="com.hwig.admin.product.ProductVO">
		select prd_id, category_id, sel_id, prd_name, prd_price, prd_comment from tbl_prd order by prd_id desc
	</select>
</mapper>